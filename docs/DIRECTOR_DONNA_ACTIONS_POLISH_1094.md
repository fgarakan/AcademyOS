# Sprint 1094 — Director DONNA Recommended Actions Polish V1

## What was built

Polished the "Next Best Actions" card in the director DONNA left column:

1. Added "Review queue" shortcut link in the card header (right-aligned, lime text)
2. Added `ChevronRight` indicator per action row so each card reads as a navigation target
3. Wrapped action text/reason in `flex-1` for correct alignment with the chevron

## Files modified

- `src/app/director/donna/page.tsx` — recommended actions card header and row layout

## Files created

- `docs/DIRECTOR_DONNA_ACTIONS_POLISH_1094.md` — sprint doc

## TypeScript

Clean.
